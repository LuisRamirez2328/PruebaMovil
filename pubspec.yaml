name: phone_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.5.0-304.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.1.10
  http: ^1.2.2  # Actualiza a una versión compatible
  font_awesome_flutter: ^10.1.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
